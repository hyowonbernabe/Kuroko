<Window x:Class="Kuroko.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        mc:Ignorable="d"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="Kuroko Toolbar" Height="50" Width="600"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        Topmost="True"
        ResizeMode="NoResize"
        ShowInTaskbar="True">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#888"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#333"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#00FF41"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#CC111111" BorderBrush="#333" BorderThickness="1" CornerRadius="25">
        <Grid MouseDown="Header_MouseDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <!-- Status -->
                <ColumnDefinition Width="*"/>
                <!-- Spacer -->
                <ColumnDefinition Width="Auto"/>
                <!-- Controls -->
                <ColumnDefinition Width="Auto"/>
                <!-- Exit -->
            </Grid.ColumnDefinitions>

            <!-- Status -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <Border x:Name="StatusIndicator" Width="8" Height="8" Background="#444" CornerRadius="4">
                    <Border.Effect>
                        <DropShadowEffect Color="#444" BlurRadius="4" Opacity="0.8"/>
                    </Border.Effect>
                </Border>
                <TextBlock x:Name="StatusText" Text="OFFLINE" Foreground="#666" FontFamily="Consolas" FontWeight="Bold" FontSize="11" VerticalAlignment="Center" Margin="10,0,0,0"/>
            </StackPanel>

            <!-- Controls -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0">
                <Button x:Name="BtnInit" Content="INITIALIZE" Click="BtnInit_Click" FontWeight="Bold"/>
                <Border Width="1" Height="14" Background="#333" Margin="5,0"/>
                <Button Content="LIVE TRANSCRIPT" Click="ToggleTranscript_Click"/>
                <Button Content="AI INSIGHT" Click="ToggleOutput_Click"/>
                <Button Content="SYSTEM CONFIG" Click="ToggleSettings_Click"/>
            </StackPanel>

            <!-- Exit -->
            <Button Grid.Column="3" Content="✕" Click="ExitButton_Click" Foreground="#844" Padding="8,0" Margin="0,0,15,0" VerticalAlignment="Center"/>
        </Grid>
    </Border>
</Window>